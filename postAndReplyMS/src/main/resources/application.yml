spring:
  application:
    name: post
  mvc:
    servlet:
      path: /post

server:
  port: 8086

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
